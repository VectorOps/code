include:
  - template: nodes/*.yaml

edges:
  - requirements.done -> validate-requirements
  - validate-requirements.success -> architect
  - validate-requirements.fail -> requirements
  - architect.done -> coder
