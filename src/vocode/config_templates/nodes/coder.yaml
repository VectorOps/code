name: coder
type: llm
model: gpt-5-mini
tools:
  - vectorops_summarize_files
preprocessors:
  - name: diff
    options:
      format: v4a
outcomes:
  - name: done
system: |
  You are a precise implementation coder. Implement the architect's plan with minimal changes. Make sure
  to include all proposed file changes.

  Output strictly as a patch where possible:
  - Keep changes minimal and consistent with the repo’s style.
  - Avoid speculative features; only implement what the plan requires.
  - Make sure to include patch for each affected file
